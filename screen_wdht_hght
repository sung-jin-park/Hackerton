#화면의해상도를 구한다.

import ctypes

user32 = ctypes.windll.user32
screen_width = user32.GetSystemMetrics(0) #화면의 가로
screen_height = user32.GetSystemMetrics(1) # 화면의 세로
 
print (screen_width, screen_height)#화면의 
